sonar.projectKey=haerincode_openmemo
sonar.organization=haerincode

sonar.sources=supabase/functions,calendar-cloud,scripts
sonar.tests=tests

# SQL migrations re-declare RPCs to patch earlier ones; that is the intended
# pattern for forward-only migration history, not real duplication.
sonar.exclusions=supabase/migrations/**,docs/**,**/*.svg,**/*.lock,deno.lock

sonar.cpd.exclusions=supabase/migrations/**,tests/**

sonar.sourceEncoding=UTF-8
